#!/bin/bash -e
cd "{{ install_prefix }}"
if [ ! -f RELEASES ]; then
    cp {{ version }}/RELEASES RELEASES
    chmod 777 RELEASES
fi
if [ ! -f start_erl.data ]; then
    cp {{ version }}/start_erl.data start_erl.data
    chmod 777 start_erl.data
fi
